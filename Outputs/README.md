# Outputs

Generated evidence and consumable optimisation decisions live here during local runs but are ignored by default in Git.

Planned artifacts include benchmark summaries, kernel/crossover selections, tuning profiles, hardware/workload profiles and certification manifests. Every artifact must carry provenance sufficient to reproduce or invalidate it.
