# Profiles

Profiles describe the environment/workload under which a candidate was measured: CPU/hardware capabilities, OS, compiler, flags, source revision, workload identity and relevant SC2 build/data version.

Profiles are evidence inputs/outputs. They are not permission to silently enable hardware-specific code; capability and net-benefit gates still apply.
