# Profiles

Hardware and workload profiles describe the context in which optimisation evidence is valid. They are inputs to certification/tuning and must not silently become universal truth.

Planned profile identities include CPU/features, compiler/build flags, SC2 build/data version, workload family and size/context buckets.
