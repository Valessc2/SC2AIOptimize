# Bench

Benchmarks compare a known baseline against one or more candidates on identical workloads. Report total cost, sample count and distribution/tail metrics where useful. Include adapter/dispatch/bookkeeping overhead rather than timing only the attractive inner loop.

A benchmark result can select **baseline/OFF**. That is expected behaviour, not a failure of the benchmark.
