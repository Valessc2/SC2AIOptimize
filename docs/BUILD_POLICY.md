# Build/toolchain policy

Release optimisation features such as IPO/LTO, PGO, ISA-specific vectorisation and parallel execution are capability/evidence-gated. Unsupported modes fail closed; representative profiles are never fabricated.
